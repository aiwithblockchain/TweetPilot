# T3-04 候选回复展示界面与生成追踪

- 所属切片：[`03.Slice-Reply Agent生成候选回复.md`](/Users/wesley/aiwithblockchain/TweetPilot/docs/phase1/slices/03.Slice-Reply%20Agent生成候选回复.md)
- 优先级：`P0`
- 任务类型：`UI + 集成`
- 依赖任务：`T3-02`, `T3-03`

## 1. 任务目标

构建 UI 触发 Reply Agent 生成并展示候选回复和追踪信息，让用户可以看到生成结果、元数据和关联关系。

## 2. 建议修改范围

优先新增：

- `/Users/wesley/aiwithblockchain/TweetPilot/src/features/reply-generation/**`

可能修改：

- `/Users/wesley/aiwithblockchain/TweetPilot/src/features/commentInput/**` （添加生成触发入口）

## 3. 详细工作内容

### 3.1 实现生成触发 UI

创建 `src/features/reply-generation/ReplyGenerationTrigger.tsx`：

功能：
- 显示当前选中的评论输入
- 显示当前账号和可选角色列表
- 提供"生成回复"按钮
- 显示生成进度状态

UI 元素：
- 评论输入预览（只读）
- 角色选择下拉框
- 生成数量选择（默认 3 个）
- "生成回复"按钮
- 加载状态指示器

### 3.2 实现候选回复展示 UI

创建 `src/features/reply-generation/CandidateReplyList.tsx`：

功能：
- 展示多个候选回复版本
- 显示每个回复的风险等级和置信度
- 支持查看回复详情

UI 元素：
- 候选回复卡片列表
- 每个卡片显示：
  - 回复内容
  - 风险等级标签（低/中/高，不同颜色）
  - 置信度进度条
  - 生成时间
  - "查看详情"按钮

### 3.3 实现生成追踪 UI

创建 `src/features/reply-generation/ReplyGenerationMetadata.tsx`：

功能：
- 展示生成元信息
- 展示关联关系

UI 元素：
- 模型来源标签
- 知识命中数
- 使用的角色名称
- 关联的评论输入（可点击跳转）
- 关联的账号信息
- 生成时间戳

### 3.4 实现主界面集成

创建 `src/features/reply-generation/ReplyGenerationView.tsx`：

布局：
```
+----------------------------------+
| 评论输入预览                      |
+----------------------------------+
| 角色选择 | 生成数量 | [生成回复]  |
+----------------------------------+
| 候选回复列表                      |
| +------------------------------+ |
| | 回复 1 | 风险: 低 | 置信: 85% | |
| +------------------------------+ |
| | 回复 2 | 风险: 中 | 置信: 72% | |
| +------------------------------+ |
| | 回复 3 | 风险: 低 | 置信: 90% | |
| +------------------------------+ |
+----------------------------------+
| 生成追踪信息                      |
+----------------------------------+
```

### 3.5 实现业务逻辑集成

创建 `src/features/reply-generation/useReplyGeneration.ts` hook：

```typescript
export function useReplyGeneration() {
  const [isGenerating, setIsGenerating] = useState(false);
  const [replies, setReplies] = useState<CandidateReply[]>([]);
  const [error, setError] = useState<string | null>(null);
  
  const generateReplies = async (
    commentInput: CommentInput,
    roleId?: string,
    count: number = 3
  ) => {
    setIsGenerating(true);
    setError(null);
    
    try {
      // 1. 调用 Reply Agent 生成
      const results = await replyAgent.generateMultipleReplies(
        commentInput,
        count,
        { role: roleId }
      );
      
      // 2. 保存到仓储
      const savedReplies = await Promise.all(
        results.map(async (result) => {
          const reply = createCandidateReply({
            commentInputId: commentInput.id,
            accountId: commentInput.accountId,
            roleId,
            workspaceId: commentInput.workspaceId,
            content: result.reply,
            riskLevel: result.riskLevel,
            confidence: result.confidence,
            modelSource: result.metadata.modelSource,
            knowledgeHits: result.metadata.knowledgeHits,
          });
          await candidateReplyRepository.save(reply);
          return reply;
        })
      );
      
      setReplies(savedReplies);
    } catch (err) {
      setError(err.message);
    } finally {
      setIsGenerating(false);
    }
  };
  
  return { generateReplies, isGenerating, replies, error };
}
```

## 4. 明确不做

- 不实现回复编辑功能
- 不实现回复执行动作
- 不实现审核流程
- 不实现回复评分或反馈
- 不实现回复对比功能
- 不实现批量生成 UI

## 5. 交付结果

完成后至少应存在：

- `src/features/reply-generation/ReplyGenerationTrigger.tsx` - 生成触发 UI
- `src/features/reply-generation/CandidateReplyList.tsx` - 候选回复列表
- `src/features/reply-generation/ReplyGenerationMetadata.tsx` - 生成追踪 UI
- `src/features/reply-generation/ReplyGenerationView.tsx` - 主界面
- `src/features/reply-generation/useReplyGeneration.ts` - 业务逻辑 hook
- E2E 测试

## 6. AI 自检清单

1. UI 是否清晰展示所有候选回复
2. UI 是否展示风险等级和置信度
3. UI 是否展示生成元信息（模型来源、知识命中数）
4. UI 是否展示关联关系（评论输入、账号、角色）
5. 生成过程是否有加载状态指示
6. 错误处理是否友好
7. 没有实现执行或审核功能

## 7. 测试代码要求

必须新增：

### 7.1 E2E 测试
- 至少 1 个 E2E 测试：验证触发生成流程
- 至少 1 个 E2E 测试：验证候选回复展示
- 至少 1 个 E2E 测试：验证生成追踪信息展示
- 至少 1 个 E2E 测试：验证角色切换影响输出

### 7.2 集成测试
- 至少 1 个集成测试：验证 useReplyGeneration hook
- 至少 1 个集成测试：验证生成结果保存到仓储

## 8. 人工验收说明

1. 打开应用，进入评论输入列表
2. 选择一条评论输入
3. 点击"生成回复"按钮
4. 验证加载状态显示
5. 验证生成完成后显示多个候选回复
6. 验证每个回复显示风险等级和置信度
7. 验证生成追踪信息显示（模型来源、知识命中数、角色）
8. 切换角色，重新生成，验证输出有差异
9. 验证错误处理（如 API Key 无效）

## 9. 完成定义

- 生成触发 UI 可用
- 候选回复展示 UI 可用
- 生成追踪 UI 可用
- 业务逻辑集成完成
- E2E 测试通过
- 人工测试通过
- UI 清晰展示所有关键信息
