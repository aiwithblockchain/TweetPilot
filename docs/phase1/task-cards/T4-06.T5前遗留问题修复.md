# T4-06: T5 前遗留问题修复

- 版本: `v0.2.0`
- 创建日期: `2026-04-11`
- 文档状态: `已完成`
- 对应切片: Phase 1 遗留问题清理
- 前置依赖: `T4-00`, `T4-01`, `T4-02`, `T4-03`, `T4-04`, `T4-05`

## 1. 任务目标

完成 T1-T4 阶段遗留的关键问题，这些问题的建议时机已到或已过，需要在进入 T5 之前完成。本任务卡整合了 4 个应该立即处理的问题，按优先级分为必做项和应该处理项。

## 2. 核心交付物

### 2.1 必做项（P0-P1）

#### 2.1.1 真实 Electron 宿主冒烟验证
- **来源**: T2-04 Code Review
- **严重程度**: 高
- **位置**: [tests/e2e/](../../../tests/e2e/)

**问题描述**:
- 当前 E2E 测试使用 RTL (React Testing Library) 模拟，而非真实的 Electron 渲染器验证
- 无法验证真实运行环境中的问题
- 建议时机明确指出应该在 Slice 3 或 Slice 4 期间完成，但现在 T4 已完成仍未解决

**实际交付**:
- 保留 `tests/e2e/ui-chromium.spec.ts` 作为浏览器 UI smoke，不再把它视为真实 Electron 宿主验证
- 将 `tests/e2e/mockInputPipeline.e2e.test.ts` 与 `tests/e2e/CommentInputList.e2e.test.tsx` 重新归类到 `tests/integration/`
- 新增 `tests/electron-smoke/smoke.mjs` 本地优先冒烟入口
- 在 `electron/main.ts` 中加入 `ELECTRON_SMOKE_TEST=1` 测试探针，通过 `webContents.executeJavaScript` 验证真实宿主加载与 preload 可用性
- 实际覆盖场景：
  - 应用启动并完成首屏渲染
  - 宿主内导航切换到 `Accounts`
  - `window.tweetOps` 与 `window.tweetOps.localBridge.getInstances()` 可用

**验收标准**:
- [x] 新增本地优先的真实 Electron 冒烟脚本
- [x] 覆盖应用启动、导航切换、preload / IPC 探针三类关键场景
- [x] 在真实 macOS 桌面环境中通过 `npm run test:electron-smoke` 人工验证
- [x] 不纳入当前 `npm run test:all`，避免把本地宿主条件误设为 CI 强门槛

#### 2.1.2 种子数据向真实数据流的平滑过渡
- **来源**: T1-03 评审
- **严重程度**: 中
- **位置**: [src/data/platformState.ts](../../../src/data/platformState.ts), [src/data/seed.ts](../../../src/data/seed.ts)

**问题描述**:
- 当前使用 `seed.ts` 提供初始化数据
- 未来需要过渡到基于 `LocalBridge` 或 API 的真实数据流
- 需要关注如何平滑过渡，避免大规模重构
- 建议时机为 Slice 4-5，T4 已完成，现在是处理的时机

**实际交付**:
1. 引入同步快照接口 `PlatformStateDataSource { getSnapshot(): PlatformState }`
2. 新增 `SeedPlatformDataSource`
3. 新增 `LocalBridgeHybridPlatformDataSource`
4. `PlatformStateManager` 改为依赖数据源而不是直接持有裸状态
5. 默认模式仍为 `seed`，并支持通过 `VITE_PLATFORM_DATA_SOURCE=local-bridge-hybrid` 显式切换
6. 不把现有 `platformState.getWorkspaces()` 一类调用改成异步，避免扩大 T5 前改动面

**验收标准**:
- [x] 实现 `PlatformStateDataSource`
- [x] 实现 `SeedPlatformDataSource` 和 `LocalBridgeHybridPlatformDataSource`
- [x] `PlatformStateManager` 支持依赖注入数据源
- [x] 支持通过环境变量切换默认数据源
- [x] 现有同步 UI 读取模型保持兼容

### 2.2 应该处理项（P2）

#### 2.2.1 状态管理的单例模式优化
- **来源**: Slice 1 Complete Review
- **严重程度**: 低
- **位置**: [src/data/platformState.ts:44](../../../src/data/platformState.ts#L44)

**问题描述**:
- 单例模式使测试隔离困难，多个测试可能共享状态
- 建议时机为"在测试变复杂前修复"，T4 已完成，测试已经变复杂

**实际交付**:
- 导出 `createPlatformState(dataSource?)`
- 保留默认单例 `platformState`
- 新增单元测试验证不同实例互不影响

**验收标准**:
- [x] 导出 `createPlatformState` 工厂函数
- [x] 保留 `platformState` 默认实例
- [x] 测试可以使用工厂函数创建独立实例
- [x] 现有调用方无需修改

#### 2.2.2 添加 React Error Boundary
- **来源**: Slice 1 Complete Review
- **严重程度**: 低
- **位置**: [src/App.tsx](../../../src/App.tsx)

**问题描述**:
- 如果任何子组件抛出错误，整个应用会崩溃，没有错误恢复机制
- T4 已完成，现在是添加的时机

**实际交付**:
- 新增通用 `src/components/ErrorBoundary.tsx`
- 在 `src/App.tsx` 中包裹 `DashboardView`
- 提供简单 fallback：错误标题、错误 message、`Try again`
- 新增单元测试验证捕获和恢复逻辑

**验收标准**:
- [x] 创建 `ErrorBoundary` 组件
- [x] 在 `App.tsx` 中包裹主要组件
- [x] 错误时显示友好的错误提示
- [x] 提供"重试"按钮恢复应用
- [x] 补充 ErrorBoundary 的单元测试

## 3. 实施步骤

### 3.1 必做项实施顺序

1. **真实 Electron 宿主冒烟验证**（最高优先级）
   - 明确放弃 `playwright._electron.launch()` 直连方案
   - 建立本地优先的主进程探针脚本
   - 重新归类伪 E2E 测试
   - 在真实桌面环境完成人工验证

2. **种子数据向真实数据流的平滑过渡**
   - 定义同步快照接口 `PlatformStateDataSource`
   - 实现 `SeedPlatformDataSource`
   - 实现 `LocalBridgeHybridPlatformDataSource`
   - 修改 `PlatformStateManager` 支持依赖注入
   - 添加数据源切换机制

### 3.2 应该处理项实施顺序

3. **状态管理的单例模式优化**
   - 导出 `createPlatformState` 工厂函数
   - 保留 `platformState` 默认实例
   - 更新测试使用工厂函数

4. **添加 React Error Boundary**
   - 创建 `ErrorBoundary` 组件
   - 在 `App.tsx` 中使用
   - 补充测试

## 4. 验收标准

### 4.1 必做项验收标准

- [x] 建立本地优先的真实 Electron 冒烟验证入口
- [x] 在真实桌面环境完成人工验证
- [x] 实现数据源抽象接口和两种实现
- [x] 支持通过配置切换默认数据源
- [x] 现有功能在两种数据源下都能正常工作

### 4.2 应该处理项验收标准

- [ ] 导出 `createPlatformState` 工厂函数
- [ ] 测试可以使用工厂函数创建独立实例
- [ ] 创建 `ErrorBoundary` 组件并在 `App.tsx` 中使用
- [ ] 错误时显示友好提示并提供恢复机制

## 5. 测试要求

### 5.1 宿主冒烟测试

- 验证真实 Electron 环境下的应用启动
- 验证导航切换
- 验证 preload API 存在
- 验证 LocalBridge IPC 可返回结果或结构化错误

### 5.2 单元测试

- 验证 `createPlatformState` 工厂函数创建独立实例
- 验证 `SeedPlatformDataSource` 和 `LocalBridgeHybridPlatformDataSource` 实现
- 验证 `ErrorBoundary` 捕获错误并显示提示

### 5.3 集成测试

- 验证数据源切换不影响现有功能
- 验证 ErrorBoundary 不影响正常渲染

## 6. 时间估算

### 6.1 必做项

- E2E 测试真实环境: 4-6 小时
- 种子数据向真实数据流过渡: 4-6 小时
- **必做项总计**: 8-12 小时

### 6.2 应该处理项

- 状态管理单例模式优化: 1-2 小时
- React Error Boundary: 1-2 小时
- **应该处理项总计**: 2-4 小时

### 6.3 总计

- **最小范围（仅必做项）**: 8-12 小时
- **完整范围（必做 + 应该处理）**: 10-16 小时

## 7. 依赖关系

**前置依赖**:
- `T4-00` 到 `T4-05` 所有任务已完成

**后续任务**:
- Slice 5 本地扩展执行通道打通

## 8. 实施建议

### 8.1 优先级排序

1. **P0**: E2E 测试真实环境（影响测试质量）
2. **P1**: 种子数据向真实数据流过渡（影响架构演进）
3. **P2**: 状态管理单例模式优化（提升测试隔离性）
4. **P2**: React Error Boundary（提升应用健壮性）

### 8.2 实施策略

- **必做项**: 必须在进入 Slice 5 之前完成
- **应该处理项**: 建议在进入 Slice 5 之前完成，但不阻塞 Slice 5 开发

### 8.3 验证记录

- `npm test -- --run tests/unit/platformState.test.ts tests/unit/platformStateDataSource.test.ts tests/unit/ErrorBoundary.test.tsx tests/integration/app.test.tsx tests/integration/statusAndMountPoints.test.tsx tests/integration/CommentInputList.integration.test.tsx tests/integration/mockInputPipeline.integration.test.ts`
- `npm run build`
- 用户在真实 macOS 桌面环境执行 `npm run test:electron-smoke`，结果为 `Electron smoke test passed.`

### 8.4 风险提示

- `test:electron-smoke` 当前是本地优先验证入口，尚未纳入 CI gate
- 数据源抽象层已经保持同步接口，但后续若引入实时数据仍需重新设计刷新策略

---

**任务卡状态**: 已完成  
**创建日期**: 2026-04-11  
**最后更新**: 2026-04-11
